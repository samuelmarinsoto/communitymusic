#ifndef TYPES_H
#define TYPES_H

enum Types{
    //is_Idling,
    is_Asking,
    is_VotingUp,
    is_VotingDown,
    is_Exiting,
    unknown
};

enum Method{
    Get,
    Remove
};

enum Rsrc{
    LIST,
    ARR
};

#endif //TYPES_H